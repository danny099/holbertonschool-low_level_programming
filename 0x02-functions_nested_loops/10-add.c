#include "holberton.h"
/**
*add - sum a two parameter
*@a: is a number
*@b: is a number
* Return: On (succes) variable suma.
* On error, dont return lowercase letters.
*/
int add(int a, int b)
{
	int suma;

	suma = a + b;
	return (suma);
}
