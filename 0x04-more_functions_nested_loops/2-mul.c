#include "holberton.h"
/**
  *mul - multiply a two parameter
  *@a: is a number
  *@b: is a number
  * Return: On (succes) variable mult.
  * On error, dont return multiply.
  */
int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
