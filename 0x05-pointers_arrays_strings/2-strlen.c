#include "holberton.h"
/**
* _strlen - return de lenght of a string
*
*@s: is a pointer
* Return: On succes lenght oa a string.
* On error.
*/

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != 0; i++)
	{
	}
	return (i);
}
